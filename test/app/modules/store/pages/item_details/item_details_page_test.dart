import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ItemDetailsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ItemDetailsPage(title: 'ItemDetails')));
    //  final titleFinder = find.text('ItemDetails');
    //  expect(titleFinder, findsOneWidget);
  });
}
