import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:Triboneira/app/modules/store/store_module.dart';

void main() {
  initModule(StoreModule());
  // StoreController store;
  //
  setUp(() {
    //     store = StoreModule.to.get<StoreController>();
  });

  group('StoreController Test', () {
    //   test("First Test", () {
    //     expect(store, isInstanceOf<StoreController>());
    //   });

    //   test("Set Value", () {
    //     expect(store.value, equals(0));
    //     store.increment();
    //     expect(store.value, equals(1));
    //   });
  });
}
