import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('StorePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(StorePage(title: 'Store')));
    //  final titleFinder = find.text('Store');
    //  expect(titleFinder, findsOneWidget);
  });
}
