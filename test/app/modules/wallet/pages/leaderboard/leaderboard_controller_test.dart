import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:Triboneira/app/modules/wallet/wallet_module.dart';

void main() {
  initModule(WalletModule());
  // LeaderboardController leaderboard;
  //
  setUp(() {
    //     leaderboard = WalletModule.to.get<LeaderboardController>();
  });

  group('LeaderboardController Test', () {
    //   test("First Test", () {
    //     expect(leaderboard, isInstanceOf<LeaderboardController>());
    //   });

    //   test("Set Value", () {
    //     expect(leaderboard.value, equals(0));
    //     leaderboard.increment();
    //     expect(leaderboard.value, equals(1));
    //   });
  });
}
