import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('LeaderboardPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(LeaderboardPage(title: 'Leaderboard')));
    //  final titleFinder = find.text('Leaderboard');
    //  expect(titleFinder, findsOneWidget);
  });
}
